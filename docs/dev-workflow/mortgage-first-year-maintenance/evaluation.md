# 評価レポート: 住宅ローン 初年度維持費の項目別トグル

評価日: 2026-07-21
対象: Codex 実装分（engine/types.ts, engine/mortgage.ts, engine/default-input.ts, components/data-input/data-input-page.tsx, lib/storage.ts, engine/__tests__/mortgage.test.ts）

## 1. 受け入れ基準チェック

| 基準 | 結果 | 備考 |
|---|---|---|
| 維持費カードに管理費・修繕積立金・保険の「初年度に含める」トグルが表示される | 合格 | 実機で3トグル確認（管理費 / 修繕積立金 / 火災保険・地震保険） |
| 管理費トグルOFF → 1年目管理費0・2年目以降不変 | 合格 | 単体テスト＋実機（1年目維持費 48.7→25.9、翌年不変） |
| 修繕積立金トグルOFF → 1年目0・2年目以降不変 | 合格 | 単体テスト（plan[0]=0, plan[1] 不変） |
| 保険トグルOFF → 1年目0・次の更新年(6年目)は満額維持 | 合格 | 単体テスト（plan[0]=0, plan[5]=火災+地震） |
| 全ON（既定）で本機能追加前と結果一致 | 合格 | 既存の維持費・回帰テストがそのまま通過（既定値は全て true） |
| 住宅ローンページ／ライフプラン明細に反映 | 合格 | 実機で住宅ローンページ 1年目維持費・初年度の月の支払いが更新（18.02万円）。明細はエンジン結果を参照 |
| 複利上昇・段階的の両モードで機能 | 合格 | compound モードの単体テスト＋UIは両モードでトグル表示 |
| エンジンに単体テスト追加・`npm test` 通過 | 合格 | 59テスト全通過（55→59、+4） |
| `tsc --noEmit` / `eslint` エラーなし | 合格 | 両方クリーン（.open-next 生成物を除去して実行） |

## 2. テスト結果

- 実行コマンド: `npx vitest run` / `npx tsc --noEmit` / `npx eslint`
- 結果: **テスト 59 件すべて通過**、型エラーなし、lint エラーなし
- 追加テスト: 管理費/修繕積立金/保険それぞれの初年度除外、compound モードでの管理費除外（計4件）

## 3. コードレビュー所見

| 重要度 | ファイル:行 | 指摘内容 |
|---|---|---|
| Low | engine/mortgage.ts:109 | 設計どおり、モード分岐で各値を算出した後に「経過1年目のみ」トグルOFF項目を0化。責務が1箇所に集約され良好。指摘なし |
| Low | components/data-input/data-input-page.tsx:854 | `FirstYearInclusionToggle` は aria-pressed / aria-label 付きで既存のセグメント型トグルと一貫。良好 |
| Low | components/data-input/data-input-page.tsx:731 | 維持費カードの「初年度の月額合計」もトグルを反映して算出。設計の想定（初年度実額表示）に沿っており、表示の一貫性が保たれている |

不要な複雑化・設計との乖離は見られない。指定外ファイル（mortgage-page.tsx 等）は未変更で、スコープを逸脱していない。

## 4. セキュリティレビュー所見

| 重要度 | ファイル:行 | 指摘内容 |
|---|---|---|
| — | — | 特筆事項なし。クライアント側の数値・真偽値計算のみで、外部入力・秘密情報・ネットワーク送信・インジェクション面はない。localStorage の扱いは既存踏襲（キーを v10 に更新） |

## 5. 総合判定

- [x] 合格（リリース準備フェーズへ進める）
- [ ] 要修正（Codexへ差し戻し）

## 6. Codexへの差し戻し内容（要修正の場合）

なし（差し戻し不要）。
