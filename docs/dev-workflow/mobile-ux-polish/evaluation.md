# 評価レポート: モバイルUXの改善（A / C）

評価日: 2026-07-22
対象: Codex 実装分（components/data-input/data-input-page.tsx, app/page.tsx）＋削除ボタン高さの微修正

## 1. 受け入れ基準チェック

| 基準 | 結果 | 備考 |
|---|---|---|
| 320px で支出タブの入力ボックスがはみ出さない | 合格 | 入力73個すべて画面内、clipped=0、pageOverflow=0 |
| 375px でもはみ出さない | 合格 | pageOverflow=0、clipped=0 |
| 全タブで横方向のページあふれが発生しない | 合格 | 320/375px で document scrollWidth == clientWidth |
| 縦積み行でもラベルが分かり追加/変更/削除できる | 合格 | 費目/夫/妻 等のフィールドラベルが表示、行=カードで操作可能 |
| 主要操作要素のタップ高さ ≒44px以上 | 合格 | 支出テーブルの削除ボタン=44px、追加ボタン=46px（下記5に軽微な補足あり） |
| デスクトップ(1280px)の見た目が実質同じ | 合格 | ヘッダ行＋整列列＋「×」アイコン＋合計行を維持、回帰なし |
| npm test / tsc / eslint 通過 | 合格 | 59テスト通過（計算不変）、型・lint エラーなし |

## 2. テスト結果

- `npx vitest run`: 59件通過（ロジック不変のため既存テストがそのまま通過）
- `npx tsc --noEmit`: 成功
- `npx eslint`: 成功（対象2ファイル・全体とも、.open-next 除去後）
- コンソールエラー: なし（実機）

## 3. コードレビュー所見

| 重要度 | ファイル:行 | 指摘内容 |
|---|---|---|
| Low | data-input-page.tsx（LivingExpense/SpecificExpense） | `<table>` を `grid grid-cols-1 sm:grid-cols-[...]` に置換。ヘッダ行とデータ行で同一テンプレートを使いデスクトップの整列を維持。既存の他行パターンと一貫。良好 |
| Low | data-input-page.tsx（RowDeleteButton 相当） | モバイル `min-h-[44px] w-full` テキスト「削除」、`sm`以上は従来の丸「×」。レスポンシブ切替で aria-label 維持 |
| Low | app/page.tsx:188,196 | モバイル上部ナビの select を `py-2.5`、バー余白を微調整。デスクトップ(lg以上)は非表示のため影響なし |

設計との乖離・不要な複雑化なし。変更は対象2ファイルのみ、計算・型・保存は不変。

## 4. セキュリティレビュー所見

| 重要度 | ファイル:行 | 指摘内容 |
|---|---|---|
| — | — | 特筆事項なし（表示のみの変更） |

## 5. 総合判定

- [x] 合格（リリース準備フェーズへ進める）
- [ ] 要修正

### 補足（スコープ外・軽微）
子育て費用・昇給フェーズ・維持費段階など既存のフェーズ行の「削除」ボタンは従来パターン（モバイル約40px,
`h-10 md:h-9`）のまま。今回のスコープ（A=支出2テーブル / C=RowDeleteButton・AddRowButton・ナビ）には
含めていない。これらも44pxに揃えたい場合は次回対応候補として提示する（機能・レイアウトの破綻はない）。

## 6. Codexへの差し戻し内容（要修正の場合）

なし（差し戻し不要。削除ボタン高さの微修正は評価中に codex-reply で反映済み）。
